{
  "name": "neuralnotes",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first macOS semantic knowledge galaxy for Apple Notes.",
  "scripts": {
    "dev": "concurrently -n api,desktop -c cyan,magenta \"npm run dev:api\" \"npm run dev:desktop\"",
    "dev:desktop": "npm --prefix apps/desktop run dev",
    "dev:api": "cd backend && uvicorn app.main:app --reload --host 127.0.0.1 --port 8717",
    "build": "npm --prefix apps/desktop run build",
    "tauri": "npm --prefix apps/desktop run tauri"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
